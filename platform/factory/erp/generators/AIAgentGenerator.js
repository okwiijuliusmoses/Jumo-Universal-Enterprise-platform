import { aiERPRegistry } from "../../../registry/ai/AIERPRegistry.js";

export class AIAgentGenerator {

 generate(blueprint){
   // The prompt mentions "AI Agent Capability Registry". Is there one?
   // AIERPRegistry seems to manage ERPs, not agents directly.
   // Let me check if there's an AIAgentRegistry.
   return [];
 }

}

export const aiAgentGenerator = new AIAgentGenerator();
